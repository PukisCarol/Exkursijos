services:
  web:
    build: .
    ports:
      - "8000:8000"
    
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: ekskursijos
      POSTGRES_DB: ekskursijos_db
    ports:
      - "5432:5432"